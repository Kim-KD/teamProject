<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardBean">

	<!-- 게스트 하우스 게시글 총 갯수 -->
	<!-- <select id='gh_Count' resultType='int'>
		select /*+ index_ffs(gh_info
		pk_gh_num)*/ count(*)
		from gh_info
	</select> -->

	<!-- 관광 명소 게시글 총 갯수 -->
	<!-- <select id='trip_Count' resultType='int'>
		select /*+
		index_ffs(trip_info pk_trip_num)*/ count(*)
		from trip_info
	</select> -->

	<!-- 게스트 하우스 게시글 정보 -->
	<!-- <select id="gh_Find_All" resultType="board"> 
 	<![CDATA[ 
 		select*from 
 			(select gh.*,rownum as rnum from 
 				(select /*+ index_desc(gh_info gh_pk_no)*/  
 				no,user_id userId,title,content,w_date wDate,city,postcode, 
 				address,on_off onOff,p_link pLink,up_view upView,views,likes 
 				from gh_info)gh 
 			where rownum<=#{endRowNum}) 
 		where rnum>=#{startRowNum} 
 	]]>
	</select> -->

	<!-- 관광 명소 게시글 정보 -->
	<!-- <select id="trip_Find_All" resultType="board"> 
 	<![CDATA[ 
 	 		select*from 
 	 			(select trip.*,rownum as rnum from 
 	 				(select /*+ index_desc(trip_info trip_pk_no)*/ 
 	 				no,user_id userId,title,content,admission,opentime,w_date wDate, 
 	 				city,postcode,address,on_off onOff,p_link pLink,up_view upView,views,likes 
 	 				from trip_info)trip 
 	 			where rownum<=#{endRowNum}) 
 	 		where rnum>=#{startRowNum} 
 	 	]]>
	</select> -->

	<!-- 게스트하우스 조회수 상승 -->
	<!-- <update id="gh_Read_Cnt">
		update gh_info set views=views+1
		where no=#{no} and
		rownum=1
	</update> -->

	<!-- 관광명소 조회수 상승 -->
	<!-- <update id="trip_Read_Cnt">
		update trip_info set views=views+1
		where no=#{no} and
		rownum=1
	</update> -->

	<!-- 게스트하우스 글번호 찾기 -->
	<!-- <select id="gh_Find_no" resultType="board">
		select no,user_id
		userId,title,content,w_date wDate,city,postcode,
		address,on_off
		onOff,p_link pLink,up_view upView,views,likes
		from gh_info
		where
		no=#{no}
	</select> -->

	<!-- 관광명소 글번호 찾기 -->
	<!-- <select id="trip_Find_no" resultType="board">
		no,user_id
		userId,title,content,admission,opentime,w_date wDate,
		city,postcode,address,on_off onOff,p_link pLink,up_view
		upView,views,likes
		from trip_info
		where no=#{no}
	</select> -->

	<!-- 게스트하우스 정보 작성 -->
	<!-- <insert id="gh_Insert" parameterType="board">
		<selectKey resultType="int" keyProperty="bno" order="BEFORE">
			select
			gh_seq.nextval from dual
		</selectKey>
		insert into gh_info(no,user_id userId,title,content,w_date
		wDate,city,postcode,
		address,on_off onOff,p_link pLink,up_view
		upView,views,likes)
		values(#{no},#{userId},#{title},#{content},sysdate,#{city},#{postcode},#{address},
		y,n,n,0,0)
	</insert> -->

	<!-- 게스트하우스 방 정보 작성 -->
	<!-- <insert id="gh_Room_Insert" parameterType="board">
		insert into
		gh_room_info(no,room,room_status
		roomStatus,price,photo,room_people
		roomPeople)
		values(#{no},#{room},#{roomStatus},#{price},#{photo
		jdbcType=VARCHAR},#{roomPeople})
	</insert> -->

	<!-- 게스트하우스 상세 정보 작성 -->
	<!-- <insert id="gh_more_Insert" parameterType="board">
		insert into
		gh_more_info(no,party_menu partyMenu,alcohol,party_time
		partyTime,off_time offTime,
		join_man joinMan,join_girl
		joinGirl,party_day
		partyDay,wifi,parking,mornig,pet,in,out)
		values(#{no},#{partyMenu},#{alcohol},#{partyTime},#{offTime},#{joinMan},#{joinGirl},#{partyDay},
		#{wifi},#{parking},#{morning},#{pet},#{in},#{out})
	</insert> -->

	<!-- 관광명소 게시글 작성 -->
	<!-- <insert id="trip_Insert" parameterType="board">
		<selectKey resultType="int" keyProperty="bno" order="BEFORE">
			select
			trip_seq.nextval from dual
		</selectKey>
		insert into trip_info(no,user_id
		userId,title,content,admission,opentime,
		w_date
		wDate,city,postcode,address,on_off onOff,p_link pLink,up_view
		upView,views,likes)
		values(#{no},#{userId},#{title},#{content},#{admission},#{opentime},sysdate,
		#{city},#{postcode},#{address},y,n,n,0,0)
	</insert> -->

	<!-- 게스트하우스 게시글 수정 -->
	<!-- <update id="gh_Update">
		update gh_info
		<trim prefix="SET" suffixOverrides=",">
			<if test="title != null">title = #{title},</if>
			<if test="content != null">content = #{content},</if>
			<if test="city != null">city = #{city},</if>
			<if test="address != null">city = #{address},</if>
			<if test="postcode != null">postcode = #{postcode},</if>
			<if test="onOff != null">on_off = #{onOff},</if>
			<if test="pLink != null">p_link = #{pLink},</if>
			<if test="upView != null">up_view = #{upView},</if>
			<if test="views > 0">views = views+1,</if>
			<if test="likes > 0">likes = likes+1,</if>
		</trim>
		where no=#{no} and rownum=1
	</update> -->

	<!-- 관광명소 게시글 수정 -->
	<!-- <update id="trip_Update">
		update trip_info
		<trim prefix="SET" suffixOverrides=",">
			<if test="title != null">title = #{title},</if>
			<if test="content != null">content = #{content},</if>
			<if test="admission != null">admission = #{admission},</if>
			<if test="opentime != null">opentime = #{opentime},</if>
			<if test="city != null">city = #{city},</if>
			<if test="address != null">city = #{address},</if>
			<if test="postcode != null">postcode = #{postcode},</if>
			<if test="onOff != null">on_off = #{onOff},</if>
			<if test="pLink != null">p_link = #{pLink},</if>
			<if test="upView != null">up_view = #{upView},</if>
			<if test="views > 0">views = views+1,</if>
			<if test="likes > 0">likes = likes+1,</if>
		</trim>
		where no=#{no} and rownum=1
	</update> -->

	<!-- 게스트하우스 게시글 삭제 -->
	<!-- <delete id="gh_Delete">
		delete from gh_info where no=#{no} and rownum=1
		delete from gh_room_info where no=#{no} and rownum=1
		delete from
		gh_more_info where no=#{no} and rownum=1
	</delete> -->

	<!-- 관광명소 게시글 삭제 -->
	<!-- <delete id="trip_Delete">
		delete from trip_info where no=#{no} and rownum=1
	</delete> -->

</mapper>